{
    "name": "nids-cdd",
    "dockerComposeFile": [
      "./compose.yml"
    ],
    "service": "dev",
    "onCreateCommand": "dos2unix -n /root/.git-credentials /root/.git-credentials2",
    "postCreateCommand": "pip install -r .devcontainer/requirements.txt",
    "postStartCommand": "git config --local credential.helper \"store --file=/root/.git-credentials2\"\n"
  }
